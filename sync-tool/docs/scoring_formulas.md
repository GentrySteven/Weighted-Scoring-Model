# Scoring Formulas Guide

Scoring uses IF/AND formulas. For Google Sheets, criteria import via IMPORTRANGE.
For Excel: Option A (linked workbook) or Option B (embedded sheet).
All scoring columns are protected during sync.
