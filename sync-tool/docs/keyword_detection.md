# Keyword Detection Guide

Format detection scans content_description, condition_description, inventory,
extent types, and top container types. Case-insensitive partial matching by default.
Enable fuzzy matching in config.yml: matching.fuzzy_enabled: true (threshold: 0-100).
